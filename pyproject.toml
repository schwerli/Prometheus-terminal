[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Prometheus"
version = "0.0.1"
requires-pytho = ">= 3.11"

[project.optional-dependencies]
test = [
  "ruff==0.6.9",
]

[tool.ruff]
indent-width = 2

[tool.ruff.lint.pydocstyle]
convention = "google"
