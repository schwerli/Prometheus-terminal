[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Prometheus"
version = "0.0.1"
requires-pytho = ">= 3.11"

[project.optional-dependencies]
test = [
  "pyink==24.8.0",
  "isort==5.13.2"
]

[tool.pyink]
pyink-indentation = 2

[tool.isort]
profile = "google"
